{
  "name": "rikiki",
  "version": "1.2.0",
  "private": true,
  "description": "Jeu de Rikiki (Oh Hell) multijoueur : créez une partie, partagez un code à 4 lettres, jouez chacun depuis son téléphone.",
  "author": "Nicolas Simon",
  "license": "UNLICENSED",
  "homepage": "https://rikiki.clixite-prod.cloud",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p shared && tsc --noEmit -p server && tsc --noEmit -p client",
    "test:ux": "node scripts/ux-tests.mjs",
    "store:assets": "node scripts/store-assets.mjs",
    "store:check": "node scripts/check-store.mjs",
    "bench:bots": "tsx scripts/bot-benchmark.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "playwright": "^1.62.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
